version: '3.9'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: nico-back:latest
    env_file:
      - .env
    environment:
      - PORT=3001
    ports:
      - "3001:3001"
